red = LONG-TERM DO  
green = TODO IN THE NEXT WEEK  
purple = TODO RIGHT NOW!!!
      

**AIMS**
 
- - [ ] Finish 9 month report
- - [ ] Come up with the next set of experiments to be completed
 
|   |   |   |
|---|---|---|
||Plan|Reflection|
|Mon|Planning & Admin (11-12)||
|Tues|||
|Weds||SciPy butter function:<br><br>- Argument checks (e.g. 0 \<= Wn \<= fs/2_<br>- Warped = 2 * fs * xp.tan(xp.pi * Wn / fs)<br>- z,p,k=buttap(n, Wn)<br>- z,p,k=lp2hp_zpk(z, p, k, wo=warped)<br>- z,p,k=bilinear_zpk(z,p,k)<br>- sos=zpk2sos(z,p,k)<br><br>m = xp.arange(-N+1, N, 2, dtype=xp.float64)  <br># Middle value is 0 to ensure an exactly real pole  <br>p = -xp.exp(1j * xp.pi * m / (2 * N))<br><br>  <br><br>buttap just places the poles evenly on the unit circle in the lefthand side of the s-plane. lp2hp_zpk adds N zeros to the transfer function, then shifts the frequency to Wn by doing z = wo/z and p = wo/p. The frequency is 'pre-warped' to compensate for the distortion that will be produced in a later step:  <br>w o = 2 f s t a n ⁡ (   𝜋 W n f s   )<br><br>  <br><br>The bilinear transform:<br><br>z n = 2 f s + s n 2 f s − s n  <br><br>Transform from zpk to sos format:<br><br>1. Find pole closest to the unit circle<br>2. Pair is with the nearest zero<br>3. Add pole/zero section to sos list and remove from zpk list|
|Thurs|Corrections:<br><br>- Reword 'Threshold calculation' section<br>- Make minor corrections to|- Made Siul corrections<br>- Started Bonisoli Method section<br>- THERE IS LITERALLY NO MENTION OF ZERO-CROSSINGS IN THE BONISOLI METHOD!!!|
|Fri|- Meeting with Tiina:<br>    - Raise workshop poster lack of progress<br>    - Raise holiday request<br>- Make corrections to Bonisoli code:<br>    - Add np.abs() into the label-sounds func<br>    - Add rms-envelope based duration and peak detection. Long RMS to find peak, short RMS to find duration<br>    - Add 128-long FFT and peak-finding||
|Sat|||
    
**Potential Small Funded Projects**
 
Conservation moss planting - [https://www.mossy.earth/projects](https://www.mossy.earth/projects)

**POST 9-MONTH TODO LIST**

- - [ ] Write a grant application for microfluidic nitrate sensors in horticulture
- - [ ] Contact: Kamrun Suravi, Kathy Steppe
- - [ ] Hydroponics setup: lights, acrylic box, pump, tubing, fertiliser, timer  
```
from pathlib import Path￼￼import matplotlib￼import matplotlib.pyplot as plt￼import numpy as np￼import scipy.signal as sg￼import soundfile as sf￼￼PACKAGE_ROOT = Path(__file__).parent.parent.parent￼DATA_DIR = Path(PACKAGE_ROOT, "tests", "data")￼DATA_ROOT = Path(￼    "/run/user/448136982/gvfs/smb-share:server=152.78.176.170,share=g_liddell/Avisoft Bioacoustics"￼)￼￼examples = {￼    "easy": {￼        "file_paths": [￼            Path(DATA_DIR, "ch1T2025-07-07_11-57-25_45_0_30_m2.wav")￼        ],￼        "start_idx": int(5.3145 * 500000),￼        "end_idx": int((5.3145 + 0.002) * 500000),￼    },￼    "hard": {￼        "file_paths": [￼            Path(DATA_DIR, "ch1T2025-07-08_08-55-47_45_90_30_m1.wav")￼        ],￼        "start_idx": int(45.037 * 500000),￼        "end_idx": int((45.037 + 0.009) * 500000),￼    },￼    "rush": {￼        "file_paths": [￼            Path(￼                DATA_ROOT,￼                "xpmt_2_rush",￼                "ch1_2025-08-20_15-52-05_0000018_p17.wav",￼            ),￼            Path(￼                DATA_ROOT,￼                "xpmt_2_rush",￼                "ch2_2025-08-20_15-52-05_0000018_p17.wav",￼            ),￼        ],￼        "start_idx": 177035297 - 250000,￼        "end_idx": 177035297 + 750000,￼    },￼    "flash": {￼        "file_paths": [￼            Path(￼                DATA_ROOT,￼                "xpmt_2_rush",￼                "ch1_2025-08-20_07-49-53_0000005_p53.wav",￼            ),￼            Path(￼                DATA_ROOT,￼                "xpmt_2_rush",￼                "ch2_2025-08-20_07-49-53_0000005_p53.wav",￼            ),￼        ],￼        "start_idx": 44254430 - 250000,￼        "end_idx": 44254430 + 750000,￼    },￼    "barley": {￼        "file_paths": [￼            Path(DATA_ROOT, "xpmt_0_barley", "ch1_2025-07-17_T0000131.wav"),￼            Path(DATA_ROOT, "xpmt_0_barley", "ch2_2025-07-17_T0000131.wav"),￼        ],￼        "start_idx": 487905405 - 250000,￼        "end_idx": 487905405 + 750000,￼    },￼    "slide": {￼        "file_paths": [￼            Path(DATA_ROOT, "xpmt_0_barley", "ch1_2025-07-17_T0000127.wav"),￼            Path(DATA_ROOT, "xpmt_0_barley", "ch2_2025-07-17_T0000127.wav"),￼        ],￼        "start_idx": 311821550 - 500000,￼        "end_idx": 311821550 + 500000,￼    },￼    "spike": {￼        "file_paths": [￼            Path(DATA_ROOT, "xpmt_0_barley", "ch1_2025-07-17_T0000127.wav"),￼            Path(DATA_ROOT, "xpmt_0_barley", "ch2_2025-07-17_T0000127.wav"),￼        ],￼        "start_idx": 679452280 - 500000,￼        "end_idx": 679452280 + 500000,￼    },￼    "bump": {￼        "file_paths": [￼            Path(DATA_ROOT, "xpmt_0_barley", "ch1_2025-07-17_T0000137.wav"),￼            Path(DATA_ROOT, "xpmt_0_barley", "ch2_2025-07-17_T0000137.wav"),￼        ],￼        "start_idx": 307037216 - 250000,￼        "end_idx": 307037216 + 750000,￼    },￼    "screech": {￼        "file_paths": [￼            Path(DATA_ROOT, "xpmt_0_barley", "ch1_2025-07-17_T0000137.wav"),￼            Path(DATA_ROOT, "xpmt_0_barley", "ch2_2025-07-17_T0000137.wav"),￼        ],￼        "start_idx": 594638226 - 250000,￼        "end_idx": 594638226 + 750000,￼    },￼    "khait": {￼        "file_paths": [￼            Path(￼                "data",￼                "doi_10_5061_dryad_jwstqjqf7__v20230403",￼                "PlantSounds",￼                "Tomato Dry",￼                "id_5_sound_5.wav",￼                # "id_4_sound_20.wav",￼            )￼        ],￼        "start_idx": 0,￼        "end_idx": 1024,￼    },￼    "clipping": {￼        "file_paths": [￼            Path(￼                "data",￼                "doi_10_5061_dryad_jwstqjqf7__v20230403",￼                "PlantSounds",￼                "Tomato Cut",￼                "id_131_sound_16.wav",￼            )￼        ],￼        "start_idx": 0,￼        "end_idx": 1024,￼    },￼    "square": {￼        "file_paths": [￼            Path(￼                "data",￼                "doi_10_5061_dryad_jwstqjqf7__v20230403",￼                "PlantSounds",￼                "Tomato Cut",￼                "id_129_sound_3.wav",￼            )￼        ],￼        "start_idx": 0,￼        "end_idx": 1024,￼    },￼    "noise_calibration_mic1": {￼        "file_paths": [￼            Path(￼                DATA_DIR,￼                "ch1T2025-07-08_13-51-55_905_90_30_m1.wav",￼            ),￼        ],￼        "start_idx": 0,￼        "end_idx": 30314640,￼    },￼    "noise_calibration_mic2": {￼        "file_paths": [￼            Path(￼                DATA_DIR,￼                "ch1T2025-07-07_14-08-14_905_90_30_m2.wav",￼            ),￼        ],￼        "start_idx": 0,￼        "end_idx": 30347408,￼    },￼    "noise_barley": {￼        "file_paths": [￼            Path(DATA_ROOT, "xpmt_0_barley", "ch1_2025-07-17_T0000149.wav"),￼            Path(DATA_ROOT, "xpmt_0_barley", "ch2_2025-07-17_T0000149.wav"),￼        ],￼        "start_idx": 150000000,￼        "end_idx": 150000000 + 40000000,￼    },￼}￼￼￼def rms(￼    s: np.ndarray, window: np.ndarray = None, hop_length: int = 1￼) -\> np.ndarray:￼    """￼    Calculates the rms envelope of a signal using a sliding window.￼    The default window type is Hann, with length of 8.￼￼    Depending on array size, bottleneck might be slower. Consider switching to:￼    `np.sqrt(sp.signal.convolve(s**2, window**2, mode='same') / N)`￼￼    Args:￼        s (np.ndarray): signal (n_channels, n_samples)￼        window (np.ndarray, optional): array representing the window. Defaults to None.￼        hop_length (int, optional): how far to slide the window. Defaults to 1.￼￼    Returns:￼        np.ndarray: the rms envelope￼    """￼    if window is None:￼        window = sg.windows.hann(8, sym=False)￼    elif isinstance(window, int):￼        window = sg.windows.hann(window, sym=False)￼    else:￼        window = np.asarray(window)￼￼    window = window.reshape(1, -1)￼    frame_length = window.shape[1]￼￼    # window = window / np.sum(window) * frame_length  # normalize window. Why though Mr. Copilot?￼    s_squared = np.square(s)￼￼    # # bottleneck route￼    # s_square_padded = np.pad(s_squared, {1: frame_length}, mode="mean")￼    # windowed_sum = bn.move_sum(s_square_padded, window=frame_length)￼￼    # scipy concolve route￼    windowed_sum = sg.convolve(s_squared, np.square(window), mode="same")￼￼    # numpy stride tricks route￼    # TODO￼￼    rms_val = np.sqrt(windowed_sum / frame_length)￼    return rms_val￼￼￼def get_dtype(s: sf.SoundFile):￼    """returns the bit depth of the subtype returned by SoundFile￼￼    TODO: There is a memory saving to be had by loading data into a type which￼    matches its bit depth. E.g. loading int16 into an 2 byte type, e.g. float16￼    get the type by calling the method `subtype`, then map it to a bit depth.￼    One could return either the bit depth or a target numpy type.￼￼    Args:￼        s (sf.SoundFile): _description_￼￼        {'PCM_S8': 'Signed 8 bit PCM',￼        'PCM_16': 'Signed 16 bit PCM',￼        'PCM_24': 'Signed 24 bit PCM',￼        'PCM_32': 'Signed 32 bit PCM',￼        'PCM_U8': 'Unsigned 8 bit PCM',￼        'FLOAT': '32 bit float',￼        'DOUBLE': '64 bit float',￼        'ULAW': 'U-Law',￼        'ALAW': 'A-Law',￼        'IMA_ADPCM': 'IMA ADPCM',￼        'MS_ADPCM': 'Microsoft ADPCM',￼        'GSM610': 'GSM 6.10',￼        'G721_32': '32kbs G721 ADPCM',￼        'G723_24': '24kbs G723 ADPCM',￼        'G723_40': '40kbs G723 ADPCM',￼        'DWVW_12': '12 bit DWVW',￼        'DWVW_16': '16 bit DWVW',￼        'DWVW_24': '24 bit DWVW',￼        'VOX_ADPCM': 'VOX ADPCM',￼        'NMS_ADPCM_16': '16kbs NMS ADPCM',￼        'NMS_ADPCM_24': '24kbs NMS ADPCM',￼        'NMS_ADPCM_32': '32kbs NMS ADPCM',￼        'DPCM_16': '16 bit DPCM',￼        'DPCM_8': '8 bit DPCM',￼        'VORBIS': 'Vorbis',￼        'OPUS': 'Opus',￼        'MPEG_LAYER_I': 'MPEG Layer I',￼        'MPEG_LAYER_II': 'MPEG Layer II',￼        'MPEG_LAYER_III': 'MPEG Layer III',￼        'ALAC_16': '16 bit ALAC',￼        'ALAC_20': '20 bit ALAC',￼        'ALAC_24': '24 bit ALAC',￼        'ALAC_32': '32 bit ALAC'}￼    """￼    subtype = s.subtype￼￼    subtype_map = {￼        "PCM_16": np.float32,￼        "PCM_32": np.float64,￼        "FLOAT": np.float32,￼        "DOUBLE": np.float64,￼    }￼￼    if subtype not in subtype_map:￼        print("Playing it safe... Defaulting to SoundFiles default dtype")￼        return np.float64￼    else:￼        return subtype_map[subtype]￼￼￼### THIS FUNCTION WOULD HAVE BEEN REALLY USEFUL TO MAKE EARLIER!!!￼def downsample(x: np.ndarray, method: str = "max") -\> np.ndarray:￼    """Downsample using method 'max' or 'rms'￼￼    This function assumes that x is in the form (n_channels, n_samples)￼￼    Args:￼        x (np.ndarray): (n_channels, n_samples)￼        method (str, optional): method to downsample. Defaults to 'max'.￼￼    Returns:￼        np.ndarray: downsampled x￼    """￼    x = np.asarray(x)￼    if x.ndim == 1:￼        x = x.reshape(1, -1)￼    if x.ndim != 2:￼        print("error x.ndim must be 1 or 2")￼        return None￼￼    max_windows = 10000￼    if x.shape[1] \<= max_windows:￼        return x￼￼    n_channels = x.shape[0]￼    window_size = ((x.shape[1] - 1) // max_windows) + 1￼    n_pad = (max_windows * window_size) - x.shape[1]￼    x_padded = np.pad(x, ((0, 0), (0, n_pad)), mode="edge")￼￼    if method == "max":￼        print(x_padded.shape)￼        x_reduced = x_padded.T.reshape(-1, window_size).max(axis=1)￼        print(x_reduced.shape)￼        x_reduced = x_reduced.reshape(-1, n_channels).T￼    elif method == "rms":￼        x_square = x_padded**2￼        print(x_square.shape)￼        x_reduced = np.sqrt(￼            (x_square.T.reshape(-1, window_size).sum(axis=1)) / window_size￼        )￼        print(x_reduced.shape)￼        x_reduced = x_reduced.reshape(-1, n_channels).T￼    else:￼        print("Invalid method. Accepts only 'max' or 'rms'.")￼    return x_reduced￼￼￼# load one of several pre-defined example clips. The amount of audio￼# is adjustable, but always centred around the same section.￼def load_example(example: str, length: int = None, whole_file=False):￼    """load one of several pre-defined example clips. The amount of audio￼    # is adjustable, but always centred around the same section.￼￼    Args:￼        example (str): 'easy', 'hard', 'rush', 'barley', 'spike','flash', 'roar'￼        length (int): total length of the sample￼    """￼    if example not in examples:￼        raise ValueError(f"Unknown example: {example}")￼￼    config = examples[example]￼    base_length = config["end_idx"] - config["start_idx"]￼￼    # length = max(length, base_length)￼    if length:￼        start_idx = config["start_idx"] + (base_length - length) // 2￼    else:￼        start_idx = config["start_idx"]￼        length = base_length￼￼    y = []￼    for file_paths in config["file_paths"]:￼        if whole_file:￼            frames, sr = sf.read(file_paths)￼        else:￼            frames, sr = sf.read(file_paths, start=start_idx, frames=length)￼        y.append(frames)￼    y = np.asarray(y)￼    id = config["file_paths"][0].stem￼    return y, sr, start_idx, id￼￼￼def plot_time_and_specgram(clip, SFT, start_idx, id, time="ms", **kwargs):￼    cbar_pad = 0.005￼    cbar_width = 0.02￼    figsize = kwargs.pop("figsize", (3.4, 2.5))￼￼    extent = SFT.extent(clip.shape[0])  # extent in seconds￼    if time == "samples":￼        t_0, t_1 = start_idx, start_idx + clip.shape[-1]￼    elif time == "ms":￼        t_0, t_1 = (t + (start_idx / SFT.fs) for t in extent[:2])￼    else:￼        raise ValueError("Invalid time unit. Accepts only 'ms' or 'samples'.")￼    extent_block = (t_0, t_1) + extent[2:]￼    print(f"extent: {extent}")￼￼    Sxx_norm = SFT.spectrogram(clip)￼￼    fig_stft, axs = plt.subplots(2, 1, figsize=figsize, sharex=True)￼    fig_stft.subplots_adjust(right=0.88)￼    axs[0].plot(￼        np.linspace(extent_block[0], extent_block[1], len(clip)),￼        clip,￼        # color="black",￼    )￼    im_stft = axs[1].imshow(￼        Sxx_norm,  # 20 * np.log10(Sxx_norm + np.finfo(np.float32).eps),￼        aspect="auto",￼        origin="lower",￼        extent=extent_block,￼        cmap="viridis",￼        interpolation="none",￼        **kwargs,￼    )￼￼    if SFT.scaling == "magnitude":￼        unit = "($U^2$)"￼    elif SFT.scaling == "psd":￼        unit = "($U^2$/Hz)"￼    else:￼        unit = ""￼￼    print(f"SFT.scaling: {SFT.scaling}, unit: {unit}")￼    bbox = axs[1].get_position()￼    cax = fig_stft.add_axes(￼        [￼            bbox.x1 + (cbar_pad * bbox.width),￼            bbox.y0,￼            cbar_width * bbox.width,￼            bbox.height,￼        ]￼    )￼    fig_stft.colorbar(im_stft, cax=cax, label=("Magnitude " + unit))￼    print(20 * np.log10(Sxx_norm.max()), 20 * np.log10(Sxx_norm.min()))￼￼    axs[0].set_ylabel("Normalised\namplitude")￼    # axs[0].set_title("Time-domain signal")￼    axs[1].yaxis.set_major_formatter(￼        matplotlib.ticker.FuncFormatter(lambda x, pos: f"{x / 1000:.0f}")￼    )￼￼    if time == "ms":￼        axs[1].xaxis.set_major_formatter(￼            matplotlib.ticker.FuncFormatter(lambda x, pos: f"{x * 1000:.1f}")￼        )￼￼    # axs[1].set_title("Spectrogram")￼    if time == "samples":￼        axs[1].set_xlabel("Time (samples)")￼    elif time == "ms":￼        axs[1].set_xlabel("Time (ms)")￼￼    axs[1].xaxis.set_major_locator(matplotlib.ticker.LinearLocator(numticks=5))￼￼    axs[1].set_ylabel("Frequency (kHz)")￼    axs[1].set_ylim(0, 100000)￼    # axs[1].set_ylim(0, 150000)￼￼    # fig_stft.text(0.0, 0.0, id, rotation="horizontal", fontsize=8)￼    return fig_stft, axs￼￼￼if __name__ == "__main__":￼    import matplotlib.pyplot as plt￼￼    print(DATA_DIR)￼    y, sr, start, id = load_example("flash", length=500000)￼    print(y.shape)￼    print(sr)￼    print(y.squeeze().shape)￼    c = rms(y, window=512)￼    print(c.shape)￼    plt.plot(y.T)￼    # plt.plot(c.T)￼    plt.show()￼from pathlib import Path￼￼import matplotlib￼import matplotlib.pyplot as plt￼import numpy as np￼import scipy.signal as sg￼import soundfile as sf￼￼PACKAGE_ROOT = Path(__file__).parent.parent.parent￼DATA_DIR = Path(PACKAGE_ROOT, "tests", "data")￼DATA_ROOT = Path(￼    "/run/user/448136982/gvfs/smb-share:server=152.78.176.170,share=g_liddell/Avisoft Bioacoustics"￼)￼￼examples = {￼    "easy": {￼        "file_paths": [￼            Path(DATA_DIR, "ch1T2025-07-07_11-57-25_45_0_30_m2.wav")￼        ],￼        "start_idx": int(5.3145 * 500000),￼        "end_idx": int((5.3145 + 0.002) * 500000),￼    },￼    "hard": {￼        "file_paths": [￼            Path(DATA_DIR, "ch1T2025-07-08_08-55-47_45_90_30_m1.wav")￼        ],￼        "start_idx": int(45.037 * 500000),￼        "end_idx": int((45.037 + 0.009) * 500000),￼    },￼    "rush": {￼        "file_paths": [￼            Path(￼                DATA_ROOT,￼                "xpmt_2_rush",￼                "ch1_2025-08-20_15-52-05_0000018_p17.wav",￼            ),￼            Path(￼                DATA_ROOT,￼                "xpmt_2_rush",￼                "ch2_2025-08-20_15-52-05_0000018_p17.wav",￼            ),￼        ],￼        "start_idx": 177035297 - 250000,￼        "end_idx": 177035297 + 750000,￼    },￼    "flash": {￼        "file_paths": [￼            Path(￼                DATA_ROOT,￼                "xpmt_2_rush",￼                "ch1_2025-08-20_07-49-53_0000005_p53.wav",￼            ),￼            Path(￼                DATA_ROOT,￼                "xpmt_2_rush",￼                "ch2_2025-08-20_07-49-53_0000005_p53.wav",￼            ),￼        ],￼        "start_idx": 44254430 - 250000,￼        "end_idx": 44254430 + 750000,￼    },￼    "barley": {￼        "file_paths": [￼            Path(DATA_ROOT, "xpmt_0_barley", "ch1_2025-07-17_T0000131.wav"),￼            Path(DATA_ROOT, "xpmt_0_barley", "ch2_2025-07-17_T0000131.wav"),￼        ],￼        "start_idx": 487905405 - 250000,￼        "end_idx": 487905405 + 750000,￼    },￼    "slide": {￼        "file_paths": [￼            Path(DATA_ROOT, "xpmt_0_barley", "ch1_2025-07-17_T0000127.wav"),￼            Path(DATA_ROOT, "xpmt_0_barley", "ch2_2025-07-17_T0000127.wav"),￼        ],￼        "start_idx": 311821550 - 500000,￼        "end_idx": 311821550 + 500000,￼    },￼    "spike": {￼        "file_paths": [￼            Path(DATA_ROOT, "xpmt_0_barley", "ch1_2025-07-17_T0000127.wav"),￼            Path(DATA_ROOT, "xpmt_0_barley", "ch2_2025-07-17_T0000127.wav"),￼        ],￼        "start_idx": 679452280 - 500000,￼        "end_idx": 679452280 + 500000,￼    },￼    "bump": {￼        "file_paths": [￼            Path(DATA_ROOT, "xpmt_0_barley", "ch1_2025-07-17_T0000137.wav"),￼            Path(DATA_ROOT, "xpmt_0_barley", "ch2_2025-07-17_T0000137.wav"),￼        ],￼        "start_idx": 307037216 - 250000,￼        "end_idx": 307037216 + 750000,￼    },￼    "screech": {￼        "file_paths": [￼            Path(DATA_ROOT, "xpmt_0_barley", "ch1_2025-07-17_T0000137.wav"),￼            Path(DATA_ROOT, "xpmt_0_barley", "ch2_2025-07-17_T0000137.wav"),￼        ],￼        "start_idx": 594638226 - 250000,￼        "end_idx": 594638226 + 750000,￼    },￼    "khait": {￼        "file_paths": [￼            Path(￼                "data",￼                "doi_10_5061_dryad_jwstqjqf7__v20230403",￼                "PlantSounds",￼                "Tomato Dry",￼                "id_5_sound_5.wav",￼                # "id_4_sound_20.wav",￼            )￼        ],￼        "start_idx": 0,￼        "end_idx": 1024,￼    },￼    "clipping": {￼        "file_paths": [￼            Path(￼                "data",￼                "doi_10_5061_dryad_jwstqjqf7__v20230403",￼                "PlantSounds",￼                "Tomato Cut",￼                "id_131_sound_16.wav",￼            )￼        ],￼        "start_idx": 0,￼        "end_idx": 1024,￼    },￼    "square": {￼        "file_paths": [￼            Path(￼                "data",￼                "doi_10_5061_dryad_jwstqjqf7__v20230403",￼                "PlantSounds",￼                "Tomato Cut",￼                "id_129_sound_3.wav",￼            )￼        ],￼        "start_idx": 0,￼        "end_idx": 1024,￼    },￼    "noise_calibration_mic1": {￼        "file_paths": [￼            Path(￼                DATA_DIR,￼                "ch1T2025-07-08_13-51-55_905_90_30_m1.wav",￼            ),￼        ],￼        "start_idx": 0,￼        "end_idx": 30314640,￼    },￼    "noise_calibration_mic2": {￼        "file_paths": [￼            Path(￼                DATA_DIR,￼                "ch1T2025-07-07_14-08-14_905_90_30_m2.wav",￼            ),￼        ],￼        "start_idx": 0,￼        "end_idx": 30347408,￼    },￼    "noise_barley": {￼        "file_paths": [￼            Path(DATA_ROOT, "xpmt_0_barley", "ch1_2025-07-17_T0000149.wav"),￼            Path(DATA_ROOT, "xpmt_0_barley", "ch2_2025-07-17_T0000149.wav"),￼        ],￼        "start_idx": 150000000,￼        "end_idx": 150000000 + 40000000,￼    },￼}￼￼￼def rms(￼    s: np.ndarray, window: np.ndarray = None, hop_length: int = 1￼) -\> np.ndarray:￼    """￼    Calculates the rms envelope of a signal using a sliding window.￼    The default window type is Hann, with length of 8.￼￼    Depending on array size, bottleneck might be slower. Consider switching to:￼    `np.sqrt(sp.signal.convolve(s**2, window**2, mode='same') / N)`￼￼    Args:￼        s (np.ndarray): signal (n_channels, n_samples)￼        window (np.ndarray, optional): array representing the window. Defaults to None.￼        hop_length (int, optional): how far to slide the window. Defaults to 1.￼￼    Returns:￼        np.ndarray: the rms envelope￼    """￼    if window is None:￼        window = sg.windows.hann(8, sym=False)￼    elif isinstance(window, int):￼        window = sg.windows.hann(window, sym=False)￼    else:￼        window = np.asarray(window)￼￼    window = window.reshape(1, -1)￼    frame_length = window.shape[1]￼￼    # window = window / np.sum(window) * frame_length  # normalize window. Why though Mr. Copilot?￼    s_squared = np.square(s)￼￼    # # bottleneck route￼    # s_square_padded = np.pad(s_squared, {1: frame_length}, mode="mean")￼    # windowed_sum = bn.move_sum(s_square_padded, window=frame_length)￼￼    # scipy concolve route￼    windowed_sum = sg.convolve(s_squared, np.square(window), mode="same")￼￼    # numpy stride tricks route￼    # TODO￼￼    rms_val = np.sqrt(windowed_sum / frame_length)￼    return rms_val￼￼￼def get_dtype(s: sf.SoundFile):￼    """returns the bit depth of the subtype returned by SoundFile￼￼    TODO: There is a memory saving to be had by loading data into a type which￼    matches its bit depth. E.g. loading int16 into an 2 byte type, e.g. float16￼    get the type by calling the method `subtype`, then map it to a bit depth.￼    One could return either the bit depth or a target numpy type.￼￼    Args:￼        s (sf.SoundFile): _description_￼￼        {'PCM_S8': 'Signed 8 bit PCM',￼        'PCM_16': 'Signed 16 bit PCM',￼        'PCM_24': 'Signed 24 bit PCM',￼        'PCM_32': 'Signed 32 bit PCM',￼        'PCM_U8': 'Unsigned 8 bit PCM',￼        'FLOAT': '32 bit float',￼        'DOUBLE': '64 bit float',￼        'ULAW': 'U-Law',￼        'ALAW': 'A-Law',￼        'IMA_ADPCM': 'IMA ADPCM',￼        'MS_ADPCM': 'Microsoft ADPCM',￼        'GSM610': 'GSM 6.10',￼        'G721_32': '32kbs G721 ADPCM',￼        'G723_24': '24kbs G723 ADPCM',￼        'G723_40': '40kbs G723 ADPCM',￼        'DWVW_12': '12 bit DWVW',￼        'DWVW_16': '16 bit DWVW',￼        'DWVW_24': '24 bit DWVW',￼        'VOX_ADPCM': 'VOX ADPCM',￼        'NMS_ADPCM_16': '16kbs NMS ADPCM',￼        'NMS_ADPCM_24': '24kbs NMS ADPCM',￼        'NMS_ADPCM_32': '32kbs NMS ADPCM',￼        'DPCM_16': '16 bit DPCM',￼        'DPCM_8': '8 bit DPCM',￼        'VORBIS': 'Vorbis',￼        'OPUS': 'Opus',￼        'MPEG_LAYER_I': 'MPEG Layer I',￼        'MPEG_LAYER_II': 'MPEG Layer II',￼        'MPEG_LAYER_III': 'MPEG Layer III',￼        'ALAC_16': '16 bit ALAC',￼        'ALAC_20': '20 bit ALAC',￼        'ALAC_24': '24 bit ALAC',￼        'ALAC_32': '32 bit ALAC'}￼    """￼    subtype = s.subtype￼￼    subtype_map = {￼        "PCM_16": np.float32,￼        "PCM_32": np.float64,￼        "FLOAT": np.float32,￼        "DOUBLE": np.float64,￼    }￼￼    if subtype not in subtype_map:￼        print("Playing it safe... Defaulting to SoundFiles default dtype")￼        return np.float64￼    else:￼        return subtype_map[subtype]￼￼￼### THIS FUNCTION WOULD HAVE BEEN REALLY USEFUL TO MAKE EARLIER!!!￼def downsample(x: np.ndarray, method: str = "max") -\> np.ndarray:￼    """Downsample using method 'max' or 'rms'￼￼    This function assumes that x is in the form (n_channels, n_samples)￼￼    Args:￼        x (np.ndarray): (n_channels, n_samples)￼        method (str, optional): method to downsample. Defaults to 'max'.￼￼    Returns:￼        np.ndarray: downsampled x￼    """￼    x = np.asarray(x)￼    if x.ndim == 1:￼        x = x.reshape(1, -1)￼    if x.ndim != 2:￼        print("error x.ndim must be 1 or 2")￼        return None￼￼    max_windows = 10000￼    if x.shape[1] \<= max_windows:￼        return x￼￼    n_channels = x.shape[0]￼    window_size = ((x.shape[1] - 1) // max_windows) + 1￼    n_pad = (max_windows * window_size) - x.shape[1]￼    x_padded = np.pad(x, ((0, 0), (0, n_pad)), mode="edge")￼￼    if method == "max":￼        print(x_padded.shape)￼        x_reduced = x_padded.T.reshape(-1, window_size).max(axis=1)￼        print(x_reduced.shape)￼        x_reduced = x_reduced.reshape(-1, n_channels).T￼    elif method == "rms":￼        x_square = x_padded**2￼        print(x_square.shape)￼        x_reduced = np.sqrt(￼            (x_square.T.reshape(-1, window_size).sum(axis=1)) / window_size￼        )￼        print(x_reduced.shape)￼        x_reduced = x_reduced.reshape(-1, n_channels).T￼    else:￼        print("Invalid method. Accepts only 'max' or 'rms'.")￼    return x_reduced￼￼￼# load one of several pre-defined example clips. The amount of audio￼# is adjustable, but always centred around the same section.￼def load_example(example: str, length: int = None, whole_file=False):￼    """load one of several pre-defined example clips. The amount of audio￼    # is adjustable, but always centred around the same section.￼￼    Args:￼        example (str): 'easy', 'hard', 'rush', 'barley', 'spike','flash', 'roar'￼        length (int): total length of the sample￼    """￼    if example not in examples:￼        raise ValueError(f"Unknown example: {example}")￼￼    config = examples[example]￼    base_length = config["end_idx"] - config["start_idx"]￼￼    # length = max(length, base_length)￼    if length:￼        start_idx = config["start_idx"] + (base_length - length) // 2￼    else:￼        start_idx = config["start_idx"]￼        length = base_length￼￼    y = []￼    for file_paths in config["file_paths"]:￼        if whole_file:￼            frames, sr = sf.read(file_paths)￼        else:￼            frames, sr = sf.read(file_paths, start=start_idx, frames=length)￼        y.append(frames)￼    y = np.asarray(y)￼    id = config["file_paths"][0].stem￼    return y, sr, start_idx, id￼￼￼def plot_time_and_specgram(clip, SFT, start_idx, id, time="ms", **kwargs):￼    cbar_pad = 0.005￼    cbar_width = 0.02￼    figsize = kwargs.pop("figsize", (3.4, 2.5))￼￼    extent = SFT.extent(clip.shape[0])  # extent in seconds￼    if time == "samples":￼        t_0, t_1 = start_idx, start_idx + clip.shape[-1]￼    elif time == "ms":￼        t_0, t_1 = (t + (start_idx / SFT.fs) for t in extent[:2])￼    else:￼        raise ValueError("Invalid time unit. Accepts only 'ms' or 'samples'.")￼    extent_block = (t_0, t_1) + extent[2:]￼    print(f"extent: {extent}")￼￼    Sxx_norm = SFT.spectrogram(clip)￼￼    fig_stft, axs = plt.subplots(2, 1, figsize=figsize, sharex=True)￼    fig_stft.subplots_adjust(right=0.88)￼    axs[0].plot(￼        np.linspace(extent_block[0], extent_block[1], len(clip)),￼        clip,￼        # color="black",￼    )￼    im_stft = axs[1].imshow(￼        Sxx_norm,  # 20 * np.log10(Sxx_norm + np.finfo(np.float32).eps),￼        aspect="auto",￼        origin="lower",￼        extent=extent_block,￼        cmap="viridis",￼        interpolation="none",￼        **kwargs,￼    )￼￼    if SFT.scaling == "magnitude":￼        unit = "($U^2$)"￼    elif SFT.scaling == "psd":￼        unit = "($U^2$/Hz)"￼    else:￼        unit = ""￼￼    print(f"SFT.scaling: {SFT.scaling}, unit: {unit}")￼    bbox = axs[1].get_position()￼    cax = fig_stft.add_axes(￼        [￼            bbox.x1 + (cbar_pad * bbox.width),￼            bbox.y0,￼            cbar_width * bbox.width,￼            bbox.height,￼        ]￼    )￼    fig_stft.colorbar(im_stft, cax=cax, label=("Magnitude " + unit))￼    print(20 * np.log10(Sxx_norm.max()), 20 * np.log10(Sxx_norm.min()))￼￼    axs[0].set_ylabel("Normalised\namplitude")￼    # axs[0].set_title("Time-domain signal")￼    axs[1].yaxis.set_major_formatter(￼        matplotlib.ticker.FuncFormatter(lambda x, pos: f"{x / 1000:.0f}")￼    )￼￼    if time == "ms":￼        axs[1].xaxis.set_major_formatter(￼            matplotlib.ticker.FuncFormatter(lambda x, pos: f"{x * 1000:.1f}")￼        )￼￼    # axs[1].set_title("Spectrogram")￼    if time == "samples":￼        axs[1].set_xlabel("Time (samples)")￼    elif time == "ms":￼        axs[1].set_xlabel("Time (ms)")￼￼    axs[1].xaxis.set_major_locator(matplotlib.ticker.LinearLocator(numticks=5))￼￼    axs[1].set_ylabel("Frequency (kHz)")￼    axs[1].set_ylim(0, 100000)￼    # axs[1].set_ylim(0, 150000)￼￼    # fig_stft.text(0.0, 0.0, id, rotation="horizontal", fontsize=8)￼    return fig_stft, axs￼￼￼if __name__ == "__main__":￼    import matplotlib.pyplot as plt￼￼    print(DATA_DIR)￼    y, sr, start, id = load_example("spike", length=1000)￼    print(y.shape)￼    print(sr)￼    print(y.squeeze().shape)￼    c = rms(y, window=512)￼    print(c.shape)￼    plt.plot(y.T)￼    # plt.plot(c.T)￼    plt.show()

c = rms(y, window=32)￼plt.plot(c.T)￼np.argmax(c)￼centre_idx = np.argmax(c)
￼plt.plot(c.T\>0.0025)￼dur = np.sum(c[1]\>0.0025)

sel = y[:,centre_idx-64:centre_idx+64]

plt.plot(sel.T)￼plt.plot(np.linspace(0, 250, 65), np.fft.rfft(sel).T)￼
peak_freq_idx = np.argmax(￼    np.abs(￼        np.fft.rfft(sel)￼    )￼)
￼peak_freq = np.linspace(0, 250000, 65)[peak_freq_idx]

c = rms(y, window=4)￼plt.plot(c.T)￼plt.plot(c.T[centre_idx-64:centre_idx+64])￼dur = np.sum(c[1]\>0.0025)￼
```